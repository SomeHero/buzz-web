.connect.clearfix(ng-if='!self.Authentication.provider.isAuthenticated()')
    h1
        | MY
        span.color-red FEED

    p.bold Connect Your Twitter Account to Create Your Personal Newsfeed

    a.btn.twitter(ng-click="self.authenticate('twitter')")
        i.fa.fa-twitter
        | Connect With Twitter

    p Once You've Connected, Any links tweeted by the people you follow on twitter Will Appear Here for Easy Scanning and Sharing
    br
    p New Links Are Updated Every 10 minutes

.clearfix(infinite-scroll="self.loadFeed(self.feed.length/10, self.user.id)", ng-if='self.Authentication.provider.isAuthenticated()')
    .deckgrid(deckgrid, source='self.feed')
        div
            .tile(ng-show="!card.type", ui-sref="article.id({ id : tile.id, url : tile.url, " +
                                                             "return : 'feed.my', headline : tile.headline })")
                .main
                    img.thumbnail(ng-src='{{ card.headline_image_url }}', set-size, height='{{card.headline_image_height}}',
                    width='{{card.headline_image_width}}')
                    a.headline {{card.headline}}
                .influencer
                    img.thumbnail(ng-src='{{ card.influencer.profile_image_url }}')
                    .handle
                        p.bold {{ card.influencer.twitter_username }}
                        p.color-red(href="") {{ card.influencer.handle }}
                        p.tweet {{ card.original_tweet }}
                .footer
                    span {{ card.created_at | relativeDate }}

            .tile.adv(ng-show="card.type=='adv'")
                h2 advertisement