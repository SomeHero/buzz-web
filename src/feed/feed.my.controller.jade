.row.connect(ng-if='!self.Authentication.provider.isAuthenticated()')
    h1
        | MY
        span.color-red FEED

    p.bold Connect Your Twitter Account to Create Your Personal Newsfeed

    a.btn.twitter(ng-click="self.authenticate('twitter')")
        i.fa.fa-twitter
        | Connect With Twitter

    p Once You've Connected, Any links tweeted by the people you follow on twitter Will Appear Here for Easy Scanning and Sharing
    br
    p New Links Are Updated Every 10 minutes

.row.feed(masonry, ng-if='self.Authentication.provider.isAuthenticated()', masonry-options="{ transitionDuration: '0.2s' }")
    .col-md-4.col-sm-6.col-xs-12.masonry-brick(ng-repeat="tile in self.feed")
        .tile
            .main(ui-sref="article.id({ id : tile.id, url : tile.url, return : 'feed.my' })")
                img.thumbnail(ng-src='{{ tile.headline_image_url }}')
                a.headline {{tile.headline}}
            .influencer
                img.thumbnail(ng-src='{{ tile.influencer.profile_image_url }}')
                p.bold {{ tile.influencer.twitter_username }}
                a.color-red(href="{{ 'https://twitter.com/'+tile.influencer.handle}}") {{ tile.influencer.handle }}
                p {{ tile.original_tweet }}

            .footer
                span {{ tile.created_at | relativeDate }}

.col-md-2.col-md-offset-5(ng-if='self.Authentication.provider.isAuthenticated()')
    btn.btn.btn-default.btn-more(ng-click='self.loadFeed(self.feed.length/10, self.user.id)') MORE BUZZ